import * as React from 'react'

import SectionContainer from './SectionContainer'

const RoadmapSection = () => {
  return (
    <SectionContainer
      title="Roadmap"
      description="List of tasks and steps we plan to take for a successful journey."
    ></SectionContainer>
  )
}

export default RoadmapSection
